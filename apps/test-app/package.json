{
  "name": "@repo/test-app",
  "version": "0.0.0",
  "private": true,
  "description": "Test app to verify cross-package imports",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "dependencies": {
    "@repo/ui": "workspace:*",
    "@repo/utils": "workspace:*",
    "@repo/logger": "workspace:*",
    "@repo/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
} 